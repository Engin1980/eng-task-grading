import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/attendanceSelfSign/self-sign/$key')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/attendanceSelfSign/self-sign/$key"!</div>
}
